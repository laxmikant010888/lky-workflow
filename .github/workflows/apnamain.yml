name: LKYRunner Push Test

on:
  push:
    branches:
      - main   
      - features/**

jobs:
  test-job:
   
    runs-on:self-hosted

    steps:
      - name: Say Hello
        run: echo " Dhondu Maharaj "
          
